services:
  - type: web
    name: nyc-exploration-game
    env: static
    buildCommand: npm install
    startCommand: npx serve -s . -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
